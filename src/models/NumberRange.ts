

export class NumberRange {
    constructor(public min: number, public max: number) { }

    // public InRange = (test: number) => {
    //     return this.min >= test && test <= this.max;
    // }
}